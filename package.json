{
  "name": "drswift-simple-site",
  "version": "1.0.0",
  "private": true,
  "description": "Dr.Swift Diagnostics public website — Cloudflare Worker SSR",
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "types": "wrangler types",
    "test": "vitest run"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20250718.0",
    "typescript": "^5.8.3",
    "vitest": "^3.2.4",
    "wrangler": "^4.112.0"
  }
}
